brewfile - [Brew formulae, cask & mas]
--------------------------------------

Help:

- http://brew.sh/
- https://github.com/Homebrew/homebrew-bundle

    brew bundle help

Setup: Make sure dotfile symlinks have been established.

    export HOMEBREW_BREWFILE=~/.config/brewfile/Brewfile

Usage:

    brew bundle

Backup:

    brew bundle dump -f ./Brewfile
